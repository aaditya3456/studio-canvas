'use client';
import type { DesignCanvas } from '../types/canvas';
type Item = DesignCanvas & { elementCount: number };
export default function SavedCanvases({ items, loading, onLoad, onDelete, onClose }: { items: Item[]; loading: boolean; onLoad: (id: string) => void; onDelete: (id: string) => void; onClose: () => void }) { return <div className="modal-backdrop" role="dialog" aria-modal="true" aria-label="Saved canvases"><section className="modal"><header><div><span>Library</span><h2>Saved canvases</h2></div><button className="icon-button" onClick={onClose} aria-label="Close library">×</button></header>{loading ? <p className="muted">Loading canvases…</p> : items.length === 0 ? <div className="empty-library">No saved canvases yet. Save your current work to create one.</div> : <div className="canvas-list">{items.map((item) => <article key={item._id}><button className="canvas-item" onClick={() => onLoad(item._id)}><strong>{item.name}</strong><small>{item.elementCount} elements · Updated {new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(item.updatedAt))}</small></button><button className="delete-item" onClick={() => onDelete(item._id)} aria-label={`Delete ${item.name}`}>Delete</button></article>)}</div>}</section></div>; }
